# HTTP requests
requests>=2.31.0

# Audio generation (procedural fallback — no API needed)
numpy>=1.24.0
scipy>=1.10.0

# AI Voice / TTS
gtts>=2.3.0
edge-tts>=6.1.0

# Image processing
Pillow>=10.2.0

# Google / YouTube
google-api-python-client>=2.118.0
google-auth>=2.28.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0

# HuggingFace Spaces client (YuE music generation + video generation)
gradio_client>=1.0.0
