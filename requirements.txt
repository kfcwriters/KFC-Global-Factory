# HuggingFace — official client (handles API endpoints automatically)
huggingface_hub>=0.23.0

# Image processing
Pillow>=10.2.0

# Google / YouTube
google-api-python-client>=2.118.0
google-auth>=2.28.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0

# HTTP (still needed by other modules)
requests>=2.31.0
